[package]
name = "kernels"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "FFI to the pulsar CUDA kernel library (nvcc at build time, Linux only)."
build = "build.rs"

[build-dependencies]
cc = "1"
