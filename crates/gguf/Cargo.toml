[package]
name = "gguf"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Zero-copy GGUF reader: header, metadata, tensor table. No tensor data is touched at parse time."

[dependencies]
